json.extract! machine, :id, :annual_maintenance_cost, :build_volume_x, :build_volume_y, :build_volume_z, :laser_power, :lifespan_years, :model_number, :name, :purchase_price, :created_at, :updated_at
json.url machine_url(machine, format: :json)
